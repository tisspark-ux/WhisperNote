# WhisperNote 의존성
# torch 는 install.bat 에서 CUDA 버전에 맞게 별도 설치됨 (여기서 제외)

# UI
gradio>=4.0.0,<5.0.0

# 오디오 녹음
sounddevice
soundfile
numpy

# 전사
whisperx

# 화자 분리 (resemblyzer + scikit-learn, 완전 오프라인)
resemblyzer
scikit-learn

# Ollama HTTP 클라이언트
requests
