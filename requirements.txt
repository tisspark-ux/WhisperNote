# WhisperNote dependencies
# torch is installed separately in install.bat with the correct CUDA index URL

# ffmpeg bundled via PyAV (no system ffmpeg binary needed)
av

# UI
# starlette<0.40.0: 0.40+ changed TemplateResponse signature breaking Gradio 4.x
gradio>=4.44.1,<5.0.0
starlette>=0.37.2,<0.40.0
pillow>=10.0.0

# Audio recording / processing
sounddevice
soundfile
numpy
scipy

# Transcription (pyannote pinned before whisperx to avoid version conflict)
pyannote.audio==3.4.0
whisperx

# Speaker diarization
webrtcvad-wheels
# resemblyzer installed via install.bat --no-deps (avoids webrtcvad build conflict)
scikit-learn

# Ollama HTTP client
requests
